'use strict';
/* NeighborTools app bootstrap.
 * Full application logic is being uploaded.
 * If you see this message for more than a moment, refresh after the next deploy.
 */
(function () {
  var app = document.getElementById('app');
  if (!app) return;
  app.innerHTML =
    '<div class="vb-center">' +
    '<div style="text-align:center;max-width:360px;padding:24px">' +
    '<div class="vb-logo vb-logo-moerk" style="justify-content:center">' +
    '<span class="vb-logomark">🔧</span>NEIGHBORTOOLS</div>' +
    '<p class="vb-muted" style="margin-top:16px">Appen lastes opp til GitHub. Oppdater siden om et øyeblikk.</p>' +
    '<p class="vb-muted" style="margin-top:8px;font-size:12px">The full app is being published. Refresh shortly.</p>' +
    '</div></div>';
})();
